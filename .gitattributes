# =============================================================================
# Git Line Ending Configuration
# =============================================================================
# Force LF line endings for all text files to prevent YAML parsing issues
# on Windows/WSL when running kubectl, sed, and other Unix tools.
#
# After adding this file, run to fix existing files:
#   git add --renormalize .
#   git commit -m "Normalize line endings"
# =============================================================================

# Default: auto-detect text files and normalize to LF
* text=auto eol=lf

# Explicitly mark script and config files as text with LF endings
*.sh        text eol=lf
*.bash      text eol=lf
*.yaml      text eol=lf
*.yml       text eol=lf
*.json      text eol=lf
*.md        text eol=lf
*.txt       text eol=lf
*.bicep     text eol=lf
*.ps1       text eol=lf

# Ensure Dockerfiles use LF
Dockerfile  text eol=lf

# Binary files - don't touch
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.ico       binary
*.pdf       binary
